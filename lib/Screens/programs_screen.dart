import 'package:flutter/material.dart';
import 'programs_overview_screen.dart'; // Import the overview screen

class ProgramsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ProgramsOverviewScreen(); // Return the widget instance
  }
}